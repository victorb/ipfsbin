machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t victorbjelkholm/ipfsbin .

test:
  override:
    - docker run -it victorbjelkholm/ipfsbin npm test
